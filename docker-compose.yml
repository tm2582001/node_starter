version: "3.8"

name: node-fork-project

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: IITG
    environment:
      # DATABASE_URL: mysql://root:secret@db:3306/task_manager
      NODE_ENV: production
    # we can run as 3000:8080 to change main operating system binding
    ports:
      - "8080:8080"
    # cpus: 2
    restart: unless-stopped

    # deploy:
    #   replicas: 4
    # endpoint_mode: vip 
    #  docker stack deploy -c docker-compose.yml myapp <- for swarm load balancing

